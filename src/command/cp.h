// File : cp.h
// Header for cp.c, contains the declaration of functions needed to process cp command

#ifndef _CP_H_
#define _CP_H_

void copy(char* args_val, int (*args_info)[2], int args_count);

void cp(char* args_val, int (*args_info)[2], int args_count);

#endif